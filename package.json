{
  "name": "Spring Trail Games",
  "description": "Website for Spring Trail Games",
  "license": "MIT",
  "scripts": {
    "develop": "gatsby develop",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "deploy": "gatsby build && gh-pages -d public -b master"
  },
  "dependencies": {
    "gatsby": "^1.9.145",
    "gatsby-link": "^1.6.32"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0"
  }
}
